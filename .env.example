# ─── Server ───
PORT=3000
CORS_ORIGIN=*
LOG_LEVEL=info

# ─── LLM Extraction ───
# Default: Gemini Flash 2.0 (free tier, 1500 req/day).
# Set EXTRACTION_LLM=claude to use Claude Sonnet 4 instead.
EXTRACTION_LLM=gemini
GEMINI_API_KEY=
# ANTHROPIC_API_KEY=   # Only needed if EXTRACTION_LLM=claude

# ─── Supabase (optional for local dev — uses in-memory store if missing) ───
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# ─── Cache ───
# Leave empty for in-memory cache (free). Set for Redis/Upstash.
REDIS_URL=

# ─── Stripe (optional — billing endpoints return 501 without it) ───
STRIPE_SECRET_KEY=
APP_DOMAIN=http://localhost:3000

# ─── API Key Config ───
API_KEY_PREFIX=sk-
